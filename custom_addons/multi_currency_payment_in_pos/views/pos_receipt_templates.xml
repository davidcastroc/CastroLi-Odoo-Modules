<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- ===================================================
         EXTENSIÓN DEL RECIBO DEL POS
         =================================================== -->
    <template id="pos_ticket_inherit_multicurrency" inherit_id="point_of_sale.TicketPaymentLines">
        <xpath expr="//tr" position="after">
            <t t-foreach="order.paymentlines" t-as="line">
                <t t-if="line.currency_amount and line.payment_currency">
                    <tr class="multi_currency_info">
                        <td colspan="3" style="text-align:right; font-size:12px; color:#555;">
                            💬 (<t t-esc="line.currency_symbol or ''"/> 
                            <t t-esc="line.currency_amount"/> 
                            <t t-esc="line.payment_currency"/>)
                        </td>
                    </tr>
                </t>
            </t>
        </xpath>
    </template>
</odoo>